# Data-quality gap register

Reviewed: **2026-09-07**. Companion to [Data lineage and transformation rules](data-lineage.md).

Current correctness limits and proposed acceptance cases for trace/evaluation decisions. Entries remain open, without delivery commitments or scope changes; deferred capacity belongs in the [wishlist](backlog.md).

Evidence labels:

- **Reproduced:** observed in a focused synthetic audit case.
- **Source review:** supported by code, without a dedicated audit regression test.
- **Existing tests:** verifies this or a related boundary, not resolution of the wider gap.
- **Live observation:** observed in the running dashboard and stored records; not a synthetic regression test.

## Register

| ID | Current gap and decision impact | Evidence / implementation | Proposed acceptance scenario |
| --- | --- | --- | --- |
| DQ-01 | User-role context, reviewer/subagent instructions, or automation can mislead human prompt counts, branching, and wait interpretation. | Reproduced with an environment-context user message; [Codex adapter](../backend/agentboard/adapters/codex.py). Already discussed in [specification §7](specification.md#7-user-inputs-and-internal-context). | Distinguish observed transport role from attributed author/origin, retaining evidence and uncertainty. A context record remains inspectable but is excluded from verified human-prompt/wait metrics. Test genuine user text containing quoted markup. |
| DQ-02 | Codex raw snapshots, hashes, physical lines, and mapping versions are retained on new imports; verified event links and complete per-field Codex import/context mappings are implemented. Non-Codex and analysis/model lineage remain open. Older evidence requires reimport. OTLP wire bytes remain unarchived. | [Raw archive semantics](data-lineage.md#35-raw-archive-and-export), [round-trip tests](../backend/tests/test_raw_traces.py), [field-lineage tests](../backend/tests/test_field_lineage.py). | Extend verified lineage beyond Codex imports; resolve each normalized value to its actual archive version and both source boundaries, including events updated by reimport. |
| DQ-03 | Unknown types are archived but silently omitted from normalized events: missing coverage can look like absent activity, including new token/lifecycle formats. | Source review plus observed unmapped `world_state`, `token_usage_record`, `agent_message`, and settings records in the inspected file. | Import report accounts for input records as mapped, intentionally ignored, unsupported, or rejected, with type/version information. A new fixture variant produces a visible coverage finding rather than silently reducing metrics. Ignoring a known mirror can be intentional and documented. |
| DQ-04 | Heuristic LLM gaps omit completion-only endings; missing tool results suppress later gaps across turns. They cannot establish complete model latency. | Reproduced prompt → completion with no response: zero LLM intervals. Source review of active/anchor/pending transitions. | Fixtures cover missing responses/results, interruption, and turn transitions. Missing coverage is explicit; estimates are not presented as complete latency. Prefer direct request evidence when available without inventing absent measurements. |
| DQ-05 | Duplicate pending IDs overwrite calls; pending state survives completion/abort/compaction/rollback, risking loss or cross-boundary pairing. | Duplicate overwrite reproduced: calls `first` and `second` use one key; only `second` survives. Remaining lifecycle behavior from source review. | Preserve both conflicting calls with an anomaly or reject clearly; verify call/turn identity and lifecycle scope. An unmatched or stale result must not silently complete the wrong call. |
| DQ-06 | Completed-event text/time/category changes are mostly ignored; rewritten/reordered files can collide by session/line/suffix. Raw versions are retained, but no normalized correction protocol exists. | Reproduced changed prompt at the same line: zero inserts, original event text retained. [Store.ingest](../backend/agentboard/store.py); existing tests cover unchanged retries and unfinished completion only. | Distinguish identical retry, append, and conflicting rewrite. Record conflict/update counts. Define a deliberate reprocessing strategy preserving external IDs/cursors or exposing a versioned replacement, with a test comparing a corrected fresh import and corrected existing database. |
| DQ-07 | Backward results are clamped, backward/equal gaps omitted, and source precision/clock quality untracked. Format migration cannot restore lost precision. | Source review; [timestamp tests](../backend/tests/test_timestamps.py) cover new exact conversion and migration preservation. | Retain original source instant and resolution, explain each adjustment, and count clock/order anomalies. Verify nanosecond and millisecond sources without implying equal precision. An old imprecise row needs source-aware correction, not just zero padding. |
| DQ-08 | Concurrent writes can change stats/export counts, bounds, and intervals between reads. Card scope differs from searched/loaded timeline scope. | Source review: multiple SELECTs without an explicit read transaction in `stats`; paged export; [UI](../frontend/app.js) search/page behavior. | Repeated reads of a named analysis snapshot produce identical data and totals during concurrent ingestion. Expose scope and coverage clearly so users know whether cards describe filtered/loaded/all events. |
| DQ-09 | Rule-based status is not verified success/confidence; exit-code patterns miss other errors. Explicit timing can still measure only transport/streaming. The OTel name-contains-`tool` heuristic also classifies internal tool-router setup spans as tools. | Source review; existing tests cover selected exit-code/error mappings and timing methods. | Define supported source-specific status evidence, retain unknown when unsupported, and test errors outside the current text pattern. Any task-success/eval score uses separate evidence from `status=ok`. |
| DQ-10 | Model/category attribution is insufficient for reproducible evaluation. Session model metadata is last-seen; classification input is a bounded conversation prefix and excludes tool evidence; classification overwrites history. External labels and dummy identity are not independently verified. | Source review of [models.py](../backend/agentboard/models.py), [external classification route](../backend/agentboard/api.py), and [session metadata mapping](../backend/agentboard/adapters/codex.py). Existing tests validate shape/dummy behavior, not ground truth. | Input selection, transcript/prompt hashes, taxonomy version, truncation, and timestamp are now recorded for internal classification runs ([lineage §10](data-lineage.md#10-model-derived-data-and-continuation); [tests](../backend/tests/test_classification.py)). Retain full input/run history and model revision for reproducible evaluation; detect stale labels after session growth. Multi-model sessions must not attribute all activity to the last model. Distinguish externally asserted results from measured/verified facts. |
| DQ-11 | Codex field origins now use persisted mapping-aware evidence; absent/fallback names and generated call IDs are explicit. Other-source display explanations still lack verified per-field input links. | [Field contract](data-lineage.md#36-per-field-codex-lineage), [backend tests](../backend/tests/test_field_lineage.py), [renderer tests](../frontend/tests/lineage.test.cjs). | Extend the shared field contract to OTLP, replay, and model operations. Keep unavailable legacy evidence explicit. |
| DQ-12 | OTel log `duration` still assumes milliseconds without unit evidence; float conversion can round. Ambiguous conversation evidence cannot identify a session reliably. | Worker-ID misuse reproduced in live Codex app-server 0.153.4 traces on 2026-09-06. `otlp-session-v3` excludes numeric worker IDs, recognizes `thread_id`, reads scope/nested identities, and reconciles ancestor or unique-trace evidence. Schema v5 separates unattributed telemetry from session counts. [Repair and limits](data-lineage.md#9-live-telemetry-mappings); [tests](../backend/tests/test_otlp_sessions.py). Existing rows need explicit repair. | Source/version-specific duration unit rules and exact supported precision; ambiguous units flagged. Keep unresolved or conflicting session evidence explicit instead of inventing identity. |
| DQ-13 | Prompt mirror matching uses text/state, while even a suppressed mirror moves the LLM anchor. Repeated event-only prompts and unusual orderings are not exhaustively covered. | Source review; [existing tests](../backend/tests/test_codex.py) cover common mirror orders, fragmented text, and repeated response-item prompts. | Test identity-aware mirror matching, repeated identical real prompts, steering within a turn, and reordered/delayed mirrors. Specify which timestamp is authoritative and explain any alternate timestamp retained. |
| DQ-14 | Supported payload validation is partial. Nested malformed shapes can fail without a precise source-line diagnostic; numeric coercion may truncate values. Store `INSERT OR IGNORE` does not report every constraint-related ignored row. | Source review of [Codex adapter](../backend/agentboard/adapters/codex.py), [domain](../backend/agentboard/domain.py), and [store](../backend/agentboard/store.py). Existing tests cover common malformed envelopes, not all variants. | Invalid nested types, fractional integer fields, explicit null IDs, and invalid required values either reject atomically with useful diagnostics or follow a documented conversion. Reports distinguish true duplicates from ignored invalid data. |

Gaps compound: model comparisons may combine missing intervals (DQ-04), last-seen model attribution (DQ-10), and changing data (DQ-08).

**2026-09-07:** the separate [token usage report](data-lineage.md#71-token-usage-and-api-value) reads an immutable raw archive, supports `token_usage_record` and legacy cumulative counters, and attributes usage from chronological model evidence. This narrows DQ-03/DQ-08/DQ-10 for that report only; normalized-event coverage, general stats consistency and classification history remain open. Missing token/model evidence and unsupported prices remain explicit.

## Analysis practice while gaps remain

Preserve original files and analyze a fixed database copy. Record code revision/source snapshot, input hashes, session/source selection, import outcomes, timing groups, and exclusions. AgentBoard does not generate this manifest; record it manually.

Prefer comparing the same source and timing method. Treat incomplete/unsupported coverage as missing evidence, not zero cost. Keep dummy runs separate from actual model runs. Verify human authorship before using prompt counts or waits as human-productivity measures. Do not derive correctness or task-success scores from an `ok` status.

Also capture session metadata/classification, metric-specific raw evidence, and query/analysis definitions; event export alone is insufficient. Codex raw archive behavior is defined in [lineage §3.5](data-lineage.md#35-raw-archive-and-export); Codex import field linkage is implemented; configurable retention and lineage for other sources, model operations, and analyses remain open.

## Change-review record

For a fix, record mapping/version, source variants, tests, metric changes, and existing-data treatment. Retain the historical finding so older semantics remain documented.

| Date | Review outcome |
| --- | --- |
| 2026-09-06 | Created from source review and focused temporary-data reproductions at the user's request for a dedicated data-correctness reference. No behavior changes or gap closures were made as part of documenting these findings. |
| 2026-09-06 | Consolidated the accepted [rollout compatibility policy](specification.md#35-codex-rollout-compatibility-policy); unsupported-record reporting, origin classification, and complete event-to-source linkage remain open. Documentation only. |
| 2026-09-06 | Dashboard verification added live evidence to DQ-12; correlation remains open and stored rows were unchanged. Fixed Timeline pagination to filter operation kinds before page limits ([regression](../backend/tests/test_api_models.py)); event-only sources now explain their coverage. Parallel membership and normalized exports are unchanged. |
| 2026-09-06 | DQ-12 worker-ID association partially corrected by `otlp-session-v2`, including explicit repair of existing decoded spans. Timeline now includes internal timed spans; they remain outside LLM/tool/wait totals. Duration units and ambiguous correlation remain open. |
| 2026-09-06 | [Grouping audit](session-grouping-review.md): previous session counts incorrectly included trace fallback groups. Schema v5 separates their identity kind and UI/API counts; v3 correlation recovers Codex `thread_id` and parent-span evidence. Ambiguous records remain explicitly unattributed. |
| 2026-09-06 | Schema v6 and `codex-jsonl-v2` add verified event archive and primary record/boundary links, with a Raw JSONL inspector tab. [Regression tests](../backend/tests/test_event_raw.py) cover reimport binding and unavailable hybrid evidence. Per-field context lineage and original OTLP wire bytes remain open. |
| 2026-09-06 | `codex-jsonl-v3` limits Raw JSONL to actual event records. Inferred intervals have no raw event record; calculation-only boundaries, including older stored links, are excluded without reimport. Recorded LLM items and tool call/result records remain available. |
| 2026-09-07 | Schema v7 and `codex-jsonl-v4` persist leaf-level Codex event/session provenance, including propagated context, inferred boundaries, fallback methods, and separate archives for hybrid fields. API/UI consume verified pointers; legacy fields require matching reimport. DQ-02/DQ-11 remain open for other sources and derived analyses. |
