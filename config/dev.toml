# Paths are relative to this config file. Codex's live endpoint stays on 4318.
environment = "dev"
host = "127.0.0.1"
port = 4319
reload = true
database = "../.agentboard/dev.db"
features = [
    "import", "raw_archive", "field_lineage", "unified_timeline",
    "parallel_groups", "token_usage", "inputs", "export",
]
model_mode = "dummy"
